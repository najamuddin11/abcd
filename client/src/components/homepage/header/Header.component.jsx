import React from 'react';
import NavbarHeader from './NavbarHeader.component';

const Header = () => {
    return (
        <header>
            <NavbarHeader/>
        </header>
    );
}
    
export default Header;
